# ── Vivado build artifacts ────────────────────────────────────────────────────
*.jou
*.log
*.str
*.tmp
*.debug
*.zip
*.vds
*.veo
*.wdf
*.vdi
*.dmp
*.rpx
*.rpt
*.backup.jou
*.backup.log

# Vivado project generated directories
*.cache/
*.hw/
*.ip_user_files/
*.runs/
*.sim/
*.gen/
.Xil/
.srcs/

# Vivado synthesis / implementation artifacts
*.dcp
*.hwdef
*_stub.v
*_stub.vhdl
*_funcsim.v
*_funcsim.vhdl

# Vivado timing / utilization reports
*_timing_summary*.rpt
*_utilization*.rpt
*_drc*.rpt
*_route_status*.rpt
*_power*.rpt
*_clock_utilization*.rpt
*_methodology*.rpt
*_bus_skew*.rpt
*_io_placed*.rpt
*_incremental_reuse*.rpt
*_control_sets*.rpt
timing_summary.rpt
tight_setup_hold_pins.txt
clockInfo.txt
*.pb
*.prm

# Bitstreams and MCS (large binary artifacts — regenerate via build TCL)
*.mcs
*.bit

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
*.egg

# ── IDE / editor ─────────────────────────────────────────────────────────────
.idea/
.vscode/
*.iml
*.iws
*.ipr
.metals/
.bsp/
.bloop/
*.class

# ── Scala / sbt ───────────────────────────────────────────────────────────────
target/
project/target/
project/project/
.ensime
.ensime_cache/
.worksheet/

# ── OS artifacts ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# ── Test artifacts ────────────────────────────────────────────────────────────
test.og
test_run_dir/

# ── Misc ──────────────────────────────────────────────────────────────────────
top.sv

# ── mkdocs generated site ─────────────────────────────────────────────────────
site/

# ── Vivado project trees (whole project dirs, not just run dirs) ───────────────
# Reference design build tree — only .xpr + empty stubs tracked, not full project
ip/vivado/xc7k480t.reference/xc7k480t_ref/
# NPU diagnostic project (xdma-minimal variant)
ip/vivado/xc7k480t/diag/npu_fpga_xdma_min/
# Migrate step project trees (build outputs only; TCL sources tracked)
ip/vivado/xc7k480t/proj_v*/
proj/

# NPU migrate bootstrapped project (regenerated by bootstrap_project.tcl)
ip/vivado/xc7k480t/proj/

.env.sh

# Python userspace driver (chisel_npu_py) build artifacts
drivers/chisel_npu_py/dist/
drivers/chisel_npu_py/build/
drivers/chisel_npu_py/*.so

*.out